function pixel_strip(string){

	number = string.substr(0,string.length-2);

	return Number(number);
	
}

function percent_strip(string){

	number = string.substr(0,string.length-1);

	return Number(number);
	
}